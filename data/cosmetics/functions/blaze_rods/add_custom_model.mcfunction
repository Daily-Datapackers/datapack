
# Adds the custom model for the cosmetics
item replace entity @s armor.head with jigsaw{CustomModelData:1000}

# Gives a tag to prevent it from constantly replacing the head item
tag @s add c.custom_model
